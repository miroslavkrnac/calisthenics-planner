import React from 'react';
import { Ionicons } from '@expo/vector-icons';
import { palette } from '@colors/palette';

export const ExerciseMenu: React.FC = () => (
	<Ionicons name="ios-trash-bin-outline" size={24} color={palette.textPrimary} />
);
